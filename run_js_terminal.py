import json

# Since AppleScript JS in Chrome was disabled, let's write out the instructions 
# for what the JS console code would execute, and what elements we found statically from the files!
# But wait, let's see if we can open Safari or use another way if needed, or if the user wants us to 
# explain how they run it, and run a static analysis checks. Let's do both! We should check if safari is available.
